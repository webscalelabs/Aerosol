# The project is GPL-v2
# Mike Nugent 20100808
# michael (at symbol) michaelnugent - org

read-mysql:
    This dir contains code to read data from the mysql binary logs and split them up into parsable SQL for the insert code 

read-mysql/decodebin.py:
    Deprecated for now.

read-mysql/splitbinlog.py
    Deprecated.

read-mysql/capture.rb
    This takes in mysqlbinlog with decode and verbose and spits on invalid (so far) sql.  Requires mysql2 and db connection to resolve table names

